# --- Neon/Postgres connection (used in src/lib/db.ts) ---
# Full connection string (prefer POSTGRES_URL for pooled/Vercel deployments)
POSTGRES_URL=postgres://username:password@ep-example-pooler.ap-southeast-1.aws.neon.tech/neondb?sslmode=require

# Non-pooled/no-SSL connection string for local development (optional)
POSTGRES_URL_NO_SSL=postgres://username:password@localhost:5432/neondb?sslmode=disable

# Individual connection fields (used if connection string is not provided)
PGHOST=ep-example.ap-southeast-1.aws.neon.tech
POSTGRES_HOST=ep-example.ap-southeast-1.aws.neon.tech
PGUSER=your_neon_user
PGPASSWORD=your_neon_password
PGDATABASE=neondb
PGPORT=5432
NEON_PROJECT_ID=your-neon-project-id

# JWT secret used in auth API routes (src/app/api/auth/*)
JWT_SECRET=replace_me_with_a_long_random_secret
